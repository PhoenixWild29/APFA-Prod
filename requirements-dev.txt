# Dev/test dependencies — NOT installed in production Docker image.
# CI test jobs install these additionally: pip install -r requirements-dev.txt
-r requirements.txt
pytest==9.0.3
mypy==1.20.2
black==26.3.1
