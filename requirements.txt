langchain==1.2.12
langchain-core==1.2.19
langchain-huggingface>=0.1.0
# langgraph 1.1.2 was broken — its internal tool_node.py imports ExecutionInfo
# and ServerInfo from langgraph.runtime, but runtime.py in 1.1.2 does not define
# those symbols. Confirmed via wheel inspection: they exist in 1.1.6's runtime.py.
# Pinning 1.1.6 (latest 1.1.x patch) — still within langchain 1.2.12's required
# range of langgraph<1.2.0,>=1.1.1.
langgraph==1.1.6
# Pin langgraph-prebuilt explicitly to prevent pip drift. tool_node.py in
# prebuilt 1.0.9 references ExecutionInfo/ServerInfo which only exist in
# langgraph 1.1.3+. Both must be kept in sync.
langgraph-prebuilt==1.0.9
# Pin ALL transitive langgraph-* siblings explicitly (per CoWork review of PR #111):
# leaving these unpinned means pip can drift any one of them into a version
# that's internally inconsistent with langgraph==1.1.6, reproducing the exact
# bug class this PR fixes. Versions captured from clean Python 3.11 resolver run
# with the above pins on 2026-04-10.
langgraph-checkpoint==4.0.1
langgraph-sdk==0.3.13
pyautogen==0.2.7
transformers==5.5.3
sentence-transformers==5.3.0
faiss-cpu>=1.7.4
deltalake==1.5.0
boto3==1.42.87
minio==7.2.20
fastapi==0.135.3
uvicorn==0.44.0
aif360==0.6.1
opentelemetry-api==1.40.0
opentelemetry-sdk==1.40.0
opentelemetry-exporter-otlp==1.40.0
python-dotenv==1.0.1
pytest==9.0.2
mypy==1.20.0
black==26.3.1
tenacity==8.2.3
cachetools==7.0.5
slowapi
numpy>=1.26.0,<3.0
prometheus-client==0.25.0
python-jose[cryptography]==3.5.0
passlib[bcrypt]==1.7.4
textblob==0.20.0
aiohttp==3.13.5
# APFA-013.2: profanity-check==1.0.3 is abandoned (last release 2019) and
# fundamentally broken on Python 3.11 — it imports `joblib` from
# `sklearn.externals`, which sklearn removed in 0.23. `alt-profanity-check` is
# the canonical community fork: same top-level package name (`profanity_check`),
# same `predict_prob`/`predict` API, same linear SVM on the same training data.
# Import lines at app/main.py:62 and app/services/query_validation_service.py:15
# remain byte-identical — this is a pure requirements.txt change with zero
# code edits.
alt-profanity-check==1.8.0
celery==5.6.2
redis==7.4.0
python-magic==0.4.27
stripe==15.0.0
SQLAlchemy>=2.0
psycopg2-binary>=2.9
alembic>=1.13
