# Dev/test dependencies — NOT installed in production Docker image.
# CI test jobs install these: pip install -r requirements-dev.txt
# Run `make lock` to regenerate requirements-dev.txt.
-r requirements.txt
pytest>=9.0
mypy>=1.20
black>=26.5
