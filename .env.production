# Production environment — nginx serves SPA and proxies API on the same domain.
# Empty VITE_API_URL means "use same-origin relative paths" which is correct
# when the frontend and API share a domain (e.g., https://apfa.secureai.dev).
VITE_API_URL=
